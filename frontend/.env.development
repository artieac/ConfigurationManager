# Used by `npm run dev` (and any other Vite command run in "development" mode).
# Covers both local machine dev and the shared dev environment.
VITE_HOME_URL=http://local.configurationmanager.alwaysmoveforward.com:5173
VITE_API_BASE_URL=http://local.api.configurationmanager.alwaysmoveforward.com:8089
